# 把官方仓库的全部分支、tag 镜像到本 fork。
# 只应放在 sync 分支上；请把 GitHub 默认分支改成 sync，否则定时任务不会跑。
# 不会删除、也不会覆盖：sync、以及 cs/* 自己的开发分支。
#
# 官方分支含 .github/workflows/，默认 GITHUB_TOKEN 不能改这些文件。
# 必须在仓库 Secrets 中配置 SYNC_UPSTREAM_TOKEN：
# Classic PAT 勾选 repo + workflow；或 fine-grained PAT 授予 Contents/Workflows 读写。

name: 同步上游全部分支

on:
  schedule:
    - cron: "*/10 * * * *"
  workflow_dispatch:

concurrency:
  group: sync-upstream
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - name: 确认已配置同步令牌
        env:
          TOKEN: ${{ secrets.SYNC_UPSTREAM_TOKEN }}
        run: |
          if [ -z "$TOKEN" ]; then
            echo "::error::缺少仓库密钥 SYNC_UPSTREAM_TOKEN。请创建带 workflow 权限的 PAT，并在 Settings → Secrets and variables → Actions 中添加。"
            exit 1
          fi

      - name: 检出 sync 分支
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          ref: sync
          token: ${{ secrets.SYNC_UPSTREAM_TOKEN }}

      - name: 镜像官方分支和 tag
        env:
          UPSTREAM: https://github.com/deepseek-ai/deepseek-harness.git
          SYNC_UPSTREAM_TOKEN: ${{ secrets.SYNC_UPSTREAM_TOKEN }}
        run: |
          set -euo pipefail

          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git remote set-url origin "https://x-access-token:${SYNC_UPSTREAM_TOKEN}@github.com/${GITHUB_REPOSITORY}.git"

          git remote add upstream "$UPSTREAM" 2>/dev/null || git remote set-url upstream "$UPSTREAM"
          git fetch --prune upstream --tags
          git fetch --prune origin

          should_skip() {
            local branch="$1"
            [ "$branch" = "HEAD" ] && return 0
            [ "$branch" = "sync" ] && return 0
            case "$branch" in
              cs/*) return 0 ;;
            esac
            return 1
          }

          git for-each-ref --format='%(refname:strip=3)' refs/remotes/upstream \
          | while read -r branch; do
              [ -z "$branch" ] && continue
              if should_skip "$branch"; then
                echo "跳过分支：${branch}"
                continue
              fi

              upstream_sha="$(git rev-parse "refs/remotes/upstream/${branch}")"
              if git rev-parse --verify "refs/remotes/origin/${branch}" >/dev/null 2>&1; then
                origin_sha="$(git rev-parse "refs/remotes/origin/${branch}")"
                if [ "$origin_sha" = "$upstream_sha" ]; then
                  echo "已是最新：${branch}"
                  continue
                fi
                echo "更新分支：${branch}"
                git push origin "refs/remotes/upstream/${branch}:refs/heads/${branch}" \
                  --force-with-lease="refs/heads/${branch}:${origin_sha}"
              else
                echo "新建分支：${branch}"
                git push origin "refs/remotes/upstream/${branch}:refs/heads/${branch}"
              fi
            done

          echo "同步 tag"
          git push origin --tags
